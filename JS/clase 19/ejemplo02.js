// Spred
const vocales = ['a', 'e', 'i'];
const vocalesCompletas = [...vocales, 'o', 'u'];

console.log(vocales); // que imprime?
console.log(vocalesCompletas); // que imprime?

// Sirve para obtener todas las propiedades de un objeto o elementos de un array